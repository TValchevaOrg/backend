# Runtime dependencies. Mirrors [project.dependencies] in pyproject.toml.
# Prefer `pip install -e .` for development; this file is for plain/CI installs
# and Docker layer caching.
fastapi>=0.115
uvicorn[standard]>=0.30
psycopg[binary]>=3.1
psycopg-pool>=3.2
pydantic>=2.7
pydantic-settings>=2.3
